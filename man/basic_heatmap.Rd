% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmaps.R, R/zzz.R
\name{basic_heatmap}
\alias{basic_heatmap}
\alias{basic.heatmap}
\title{Basic clustered heatmap}
\usage{
basic_heatmap(
  df,
  Col.cluster = FALSE,
  Row.cluster = FALSE,
  title.var = "",
  colsepvar = NULL,
  row.size = 0.4,
  scale.var = "row",
  dist.method = "euclidean",
  hclust.method = "ward.D"
)

basic.heatmap(
  df,
  Col.cluster = FALSE,
  Row.cluster = FALSE,
  title.var = "",
  colsepvar = NULL,
  row.size = 0.4,
  scale.var = "row",
  dist.method = "euclidean",
  hclust.method = "ward.D"
)
}
\arguments{
\item{df}{A numeric data frame or matrix (features x samples).}

\item{Col.cluster, Row.cluster}{Logical; whether to cluster columns / rows.}

\item{title.var}{Plot title.}

\item{colsepvar}{Optional column indices at which to draw separators.}

\item{row.size}{Row-label size (\code{cex}).}

\item{scale.var}{One of \code{"row"}, \code{"column"}, \code{"none"}.}

\item{dist.method}{Distance metric, see \code{\link[stats:dist]{stats::dist()}}.}

\item{hclust.method}{Agglomeration method, see \code{\link[stats:hclust]{stats::hclust()}}.}
}
\value{
Called for its side effect (draws a heatmap); invisibly returns the
\code{\link[gplots:heatmap.2]{gplots::heatmap.2()}} object.
}
\description{
Draws a log2-scaled, hierarchically-clustered heatmap via \code{\link[gplots:heatmap.2]{gplots::heatmap.2()}}.
}
\examples{
\dontrun{
basic_heatmap(my_matrix, Col.cluster = TRUE, Row.cluster = TRUE)
}
}
