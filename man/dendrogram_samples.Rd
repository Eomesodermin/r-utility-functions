% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dendrogram.R, R/zzz.R
\name{dendrogram_samples}
\alias{dendrogram_samples}
\alias{dendrogram}
\title{Sample dendrogram}
\usage{
dendrogram_samples(
  data.matrix,
  hc_metric.var = "euclidean",
  hc_method.var = "ward.D2",
  type.var = NULL,
  output.dir = "results/figures/"
)

dendrogram(
  data.matrix,
  hc_metric.var = "euclidean",
  hc_method.var = "ward.D2",
  type.var = NULL,
  output.dir = "results/figures/"
)
}
\arguments{
\item{data.matrix}{Numeric matrix (features x samples).}

\item{hc_metric.var}{Distance metric for clustering (e.g. \code{"euclidean"}).}

\item{hc_method.var}{Agglomeration method (e.g. \code{"ward.D2"}).}

\item{type.var}{Optional \code{\link[ape:plot.phylo]{ape::plot.phylo()}} layout (\code{"phylogram"}, \code{"fan"},
\code{"unrooted"}, \code{"radial"}); if \code{NULL} only the standard dendrogram is drawn.}

\item{output.dir}{Directory to write the PDF(s) into (created if needed).}
}
\value{
Invisibly \code{NULL}; called for the PDF file(s) it writes.
}
\description{
Hierarchically clusters the samples (columns of \code{data.matrix}) and writes a
dendrogram to PDF; optionally also a phylogeny-style plot.
}
\examples{
\dontrun{
dendrogram_samples(my_matrix, type.var = "unrooted")
}
}
