% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcano.R, R/zzz.R
\name{clean_volcano_data}
\alias{clean_volcano_data}
\alias{clean.data.volcano}
\title{Prepare a table for volcano plotting}
\usage{
clean_volcano_data(input.data, sig.only = TRUE)

clean.data.volcano(input.data, sig.only = TRUE)
}
\arguments{
\item{input.data}{Data frame with \code{FDR} and \code{logFC} columns.}

\item{sig.only}{If \code{TRUE}, keep only rows with \code{FDR < 0.05}.}
}
\value{
A data frame with \code{FDR} and \code{logFC}.
}
\description{
Keeps just the \code{FDR} and \code{logFC} columns (optionally only significant rows).
}
\examples{
df <- data.frame(FDR = c(0.01, 0.2), logFC = c(2, -0.3),
                 row.names = c("A", "B"))
clean_volcano_data(df, sig.only = TRUE)
}
