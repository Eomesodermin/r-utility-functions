% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single_cell.R, R/zzz.R
\name{umap_optimise}
\alias{umap_optimise}
\alias{UMAP.optimise}
\title{Optimise UMAP hyper-parameters for a Seurat object}
\usage{
umap_optimise(
  input.seurat,
  output.dir = "results/optimising_UMAP/",
  init.min.dist = 0.001,
  init.neigh = 5,
  min.dist.step = 2,
  neigh.step = 20
)

UMAP.optimise(
  input.seurat,
  output.dir = "results/optimising_UMAP/",
  init.min.dist = 0.001,
  init.neigh = 5,
  min.dist.step = 2,
  neigh.step = 20
)
}
\arguments{
\item{input.seurat}{A Seurat object with a \code{pca} reduction.}

\item{output.dir}{Directory to write the PDFs into (created if needed).}

\item{init.min.dist}{Starting \code{min.dist} (sensible range 0.001-0.5).}

\item{init.neigh}{Starting \code{n.neighbors} (sensible range 5-50).}

\item{min.dist.step}{Multiplicative step for \code{min.dist}.}

\item{neigh.step}{Additive step for \code{n.neighbors}.}
}
\value{
Invisibly \code{NULL}; called for the PDFs it writes.
}
\description{
Sweeps \code{min.dist} and \code{n.neighbors}, saving a UMAP plot PDF for each
combination so the best-looking embedding can be chosen.
}
\examples{
\dontrun{
umap_optimise(pbmc_small)
}
}
