% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R, R/zzz.R
\name{go_enrichment}
\alias{go_enrichment}
\alias{GO.function}
\title{Gene Ontology over-representation analysis}
\usage{
go_enrichment(
  markers,
  topn = 1000,
  org = c("human", "mouse"),
  title.var = "",
  plot = TRUE,
  ontology.type = "BP",
  ...
)

GO.function(
  markers,
  topn = 1000,
  org = c("human", "mouse"),
  title.var = "",
  plot = TRUE,
  ontology.type = "BP",
  ...
)
}
\arguments{
\item{markers}{Data frame with a \code{gene} (SYMBOL) column and an \code{FDR} column.}

\item{topn}{Number of top genes (by \code{FDR}) to test.}

\item{org}{Either \code{"human"} or \code{"mouse"}.}

\item{title.var}{Plot title.}

\item{plot}{If \code{TRUE}, return a ggplot of the top terms; if \code{FALSE}, return the
\code{enrichResult} object.}

\item{ontology.type}{GO ontology to test: \code{"BP"}, \code{"MF"}, or \code{"CC"}.}

\item{...}{Further arguments passed to \code{\link[clusterProfiler:enrichGO]{clusterProfiler::enrichGO()}}.}
}
\value{
A ggplot (if \code{plot = TRUE}) or a clusterProfiler \code{enrichResult}.
}
\description{
Runs a GO over-representation test (via \code{\link[clusterProfiler:enrichGO]{clusterProfiler::enrichGO()}}) on the
top-ranked genes of a marker table, and either plots the top terms or returns
the full result.
}
\examples{
\dontrun{
go_enrichment(my_markers, org = "human", ontology.type = "BP")
}
}
