% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize.R
\name{standardize}
\alias{standardize}
\title{Row-wise robust standardisation}
\usage{
standardize(z, na.rm = TRUE)
}
\arguments{
\item{z}{A numeric matrix (features x samples).}

\item{na.rm}{Whether to ignore \code{NA}s when computing the median and MAD.}
}
\value{
A matrix the same shape as \code{z}, robustly standardised by row.
}
\description{
Centres each row of a matrix by its median and scales by its median absolute
deviation (MAD) — a robust alternative to a z-score. Rows with zero MAD
(constant values) are returned centred (all zero) instead of \code{NaN}.
}
\examples{
m <- matrix(c(1, 2, 3, 10, 20, 30), nrow = 2, byrow = TRUE)
standardize(m)
# a constant row no longer produces NaN:
standardize(rbind(c(5, 5, 5), c(1, 2, 3)))
}
